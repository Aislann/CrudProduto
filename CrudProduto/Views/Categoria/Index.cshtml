@model IEnumerable<CrudProduto.Models.Categorium>

@{
    ViewData["Title"] = "Index";
}

<h1 class="text-center">Categorias</h1>

<p>
    <a asp-action="Create" class="btn btn-success">Criar Novo</a>
</p>
<table class="table" id="tableCategorias">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Categoria)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Categoria)
            </td>
            <td>
                    <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.IdCategoria">Editar</a> |
                    <a class="btn btn-success" asp-action="Details" asp-route-id="@item.IdCategoria">Detalhes</a> |
                    <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.IdCategoria">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
